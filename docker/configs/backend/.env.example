# Docker 环境 Backend 环境变量示例
# ⚠️ 此文件在构建时 COPY 进容器（docker/backend/Dockerfile 第 26 行）
# 使用方法：cp docker/configs/backend/.env.example docker/configs/backend/.env
#
# Docker 环境下 backend 容器内的密钥注入；与 backend/.env 字段一致
# 留空则容器内 backend 自动回退到本地磁盘存储

# ─── 七牛云对象存储（可选） ───
QINIU_ACCESS_KEY=
QINIU_SECRET_KEY=
