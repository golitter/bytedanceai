## 1. 契约目录初始化

- [x] 1.1 创建 `contracts/schemas/` 和 `contracts/logs/` 目录，添加 `contracts/.gitkeep` 确保空目录可提交
- [x] 1.2 从 agentend 现有代码逆向提取 `contracts/schemas/event-types.yaml`（EventType 枚举 + StreamEvent 结构）
- [x] 1.3 逆向提取 `contracts/schemas/agent-request.yaml`（AgentRequest 结构 + AgentType 枚举）
- [x] 1.4 逆向提取 `contracts/schemas/agent-response.yaml`（AgentResponse 结构）
- [x] 1.5 逆向提取 `contracts/schemas/session-state.yaml`（SessionState 枚举 + 合法转换表）
- [x] 1.6 验证所有 YAML Schema 文件格式正确（通过 Python yaml.safe_load 校验）

## 2. 代码生成管线

- [x] 2.1 创建 `scripts/generate_contracts.py`，读取 `contracts/schemas/*.yaml` 生成三端类型文件
- [x] 2.2 实现生成 Python 类型：`contracts/schemas/event-types.yaml` → `agentend/src/generated/events.py`（Pydantic BaseModel + Enum）
- [x] 2.3 实现生成 TypeScript 类型：`contracts/schemas/event-types.yaml` → `frontend/src/generated/events.ts`（union type + interface）
- [x] 2.4 实现生成 Go 类型：`contracts/schemas/event-types.yaml` → `backend/internal/generated/events.go`（const type + struct）
- [x] 2.5 为 AgentRequest / AgentResponse / SessionState 重复上述三端生成逻辑
- [x] 2.6 每个生成文件添加 `// Code generated by make generate. DO NOT EDIT.` 头注释
- [x] 2.7 在根 Makefile 中添加 `generate` target，调用 `python scripts/generate_contracts.py`
- [x] 2.8 运行 `make generate`，验证三端生成文件正确

## 3. AgentEnd 迁移到生成类型

- [x] 3.1 修改 `agentend/src/schemas/events.py`，改为从 `src/generated/events.py` re-export EventType 和 StreamEvent
- [x] 3.2 修改 `agentend/src/schemas/request.py`，改为从 `src/generated/request.py` re-export AgentType 和 AgentRequest
- [x] 3.3 修改 `agentend/src/schemas/response.py`，改为从 `src/generated/response.py` re-export AgentResponse
- [x] 3.4 修改 `agentend/src/session/models.py`，SessionState 从 `src/generated/session.py` 导入
- [x] 3.5 验证 agentend 所有 import 路径正确，现有测试通过
- [x] 3.6 将 `agentend/src/generated/` 添加到 agentend 的 Python path 配置

## 4. pre-commit hook

- [x] 4.1 创建 `.husky/pre-commit` 脚本
- [x] 4.2 实现阶段 1：变更检测——`git diff --cached --name-only` 匹配契约相关目录模式
- [x] 4.3 实现契约一致性检查：对比各端代码中的枚举值与 `contracts/schemas/` 中的定义
- [x] 4.4 实现阶段 2：当检测到契约变更时输出结构化引导信息（变更文件列表、对比步骤、logs/ 写入要求）
- [x] 4.5 实现阶段 3：secret 验证——读取预置 secret，对比用户输入
- [x] 4.6 确保三个阶段的顺序正确：先检测 → 引导 → secret
- [x] 4.7 测试 pre-commit hook：模拟契约变更验证引导信息输出
- [x] 4.8 测试 pre-commit hook：验证非契约变更直接通过到 secret 阶段

## 5. 文档与配置

- [x] 5.1 创建 `contracts/AGENTS.md`，说明 contracts/ 的作用、规则、make generate 用法
- [x] 5.2 更新根 `AGENTS.md`，添加契约优先原则和 contracts/ 引用
- [x] 5.3 更新 `docs/common/makefile-guide.md`，添加 `make generate` 命令说明
- [x] 5.4 创建初始 `contracts/logs/` 变更记录，记录本次逆向提取的动作
