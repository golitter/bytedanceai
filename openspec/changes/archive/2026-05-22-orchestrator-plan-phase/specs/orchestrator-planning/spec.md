## ADDED Requirements

### Requirement: Orchestrator accepts user message and agent list
OrchestratorAdapter SHALL accept a user message string and a list of available agents (each with id, name, capabilities) via the standard `stream_chat` / `chat` interface. The agents list and shared directory path SHALL be passed through `kwargs` from `request.config`.

#### Scenario: Valid orchestrator request
- **WHEN** a request is sent to `/v1/agent/execute` with `agent_type: "orchestrator"` and `config` containing `agents`, `task_id`, `shared_dir`
- **THEN** OrchestratorAdapter SHALL receive `agents`, `task_id`, `shared_dir` in its kwargs and begin planning

#### Scenario: Missing required config fields
- **WHEN** a request is sent with `agent_type: "orchestrator"` but `config` is missing `task_id` or `shared_dir`
- **THEN** the adapter SHALL raise a KeyError which the API layer surfaces as a 500 error

### Requirement: Orchestrator generates structured task plan via LLM
Orchestrator SHALL use a single LLM call with `with_structured_output(PlanOutput)` to generate an execution plan containing an overview string and a list of TaskDef objects (id, agent, title, content). The LLM call SHALL be made within a LangGraph `plan` node.

#### Scenario: Successful plan generation
- **WHEN** the user message is "用 Claude Code 写登录页，用 OpenCode 审查代码" and agents list contains claude-code and opencode
- **THEN** the LLM SHALL return a PlanOutput with overview text and at least one TaskDef per mentioned agent

#### Scenario: Single agent request
- **WHEN** the user message only requires one agent
- **THEN** the LLM SHALL return a PlanOutput with exactly one TaskDef

### Requirement: Orchestrator writes task files to shared directory
Orchestrator SHALL write three types of files to `shared/.agent/`:
1. `config.yaml` — task index with task_id and list of tasks (id, agent, file)
2. `overview.md` — overall plan description
3. `tasks/task-NNN.md` — individual task markdown files, numbered sequentially by backend

File names SHALL be generated by the backend as `task-{idx:03d}.md`, never from LLM output. The write SHALL happen in a LangGraph `write_shared` node.

#### Scenario: Files written correctly
- **WHEN** the plan node produces 2 tasks (task-001 for claude-code, task-002 for opencode)
- **THEN** the following files SHALL exist under `shared_dir`:
  - `config.yaml` containing `task_id` and `tasks` list with 2 entries
  - `overview.md` containing the plan overview text
  - `tasks/task-001.md` starting with `# {title}` followed by task content
  - `tasks/task-002.md` starting with `# {title}` followed by task content

#### Scenario: shared_dir does not exist
- **WHEN** the provided `shared_dir` path does not exist
- **THEN** the write_shared node SHALL create it (including parent directories) and write files normally

### Requirement: config.yaml is declarative task index only
The config.yaml written by Orchestrator SHALL contain only `task_id` (string) and `tasks` (list of {id, agent, file}). It SHALL NOT contain runtime state such as status, session_id, results, or timestamps. Array order represents execution order.

#### Scenario: config.yaml structure
- **WHEN** Orchestrator writes config.yaml for a 2-task plan
- **THEN** the file SHALL parse as valid YAML with exactly two top-level keys: `task_id` and `tasks`, and each task entry SHALL have exactly three keys: `id`, `agent`, `file`
